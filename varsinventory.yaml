- name: variables from inventory
  hosts: local
  connection: local
  tasks: 
  - name: print course info 
    ansible.builtin.debug:
      msg: "Hello iam learning {{ course }} time {{ duration }} trained by {{ trainer }}"